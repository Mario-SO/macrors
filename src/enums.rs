#[derive(Clone, Copy)]
pub enum Exercise {
    Sedentary,
    Light,
    Moderate,
    Heavy,
}

pub enum Goal {
    Lose,
    Maintain,
    Gain,
}
#[derive(Clone, Copy)]
pub enum Gender {
    Male,
    Female,
}
